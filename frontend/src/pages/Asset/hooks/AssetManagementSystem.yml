openapi: '3.0.3'
info:
  title: API Title
  version: '1.0'
servers:
  - url: https://api.server.test/v1
paths:
  /assets:
    get:
      responses:
        '200':
          description: OK
    delete:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                Ids:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: OK
